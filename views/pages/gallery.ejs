<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs')%>
</head>

<body>
    <%- include('../partials/navbar.ejs')%>
    <section id="info" class="d-flex align-item-center justify-content-center">
        <div class="container">
            <div class="page-header">
                <h1>Фото</h1>
            </div>
            <div>
                    <% photos.forEach(function(image) { %>
                        <a data-fslightbox  href="./images/gallery/photos/<%= image %>">
                            <img class="rounded" src="./images/gallery/thumbs/<%= image %>">
                        </a>
                        <% }); %>
            </div>
        </div>
    </section>

        <%- include('../partials/footer.ejs')%>
</body>

</html>